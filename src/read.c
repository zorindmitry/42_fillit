#include "fillit.h"

////



t_list	*read_in_list(int fd)
{
	char	name;
	char	*buff;
	t_list	*list;
	int		quantity_of_tetrimins

	buff = ft_strnew(21);
	name = 'A';

	while (quantity_of_tetrimins = read(fd, buf, 21))
}
